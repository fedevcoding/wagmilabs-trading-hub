function breakEven() {
  // let calculation = (((num1 * num2  + num3 + num4) * 100) / (100 - (royalties + num5)));
  // calculation = Math.ceil(calculation * 100000) / 100000.00000;
}

function profit() {}

module.exports = {
  breakEven,
  profit,
};
